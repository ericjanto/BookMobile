/**
 * Enum class to specify order of data values in file line.
 *
 * NOTE: Do not change the order of enums unintentionally since
 * relevant indices are obtained by the java.lang.Enum.ordinal() method.
 */
public enum DataOrder {
    TITLE,
    AUTHORS,
    RATING,
    ISBN,
    PAGES
}
